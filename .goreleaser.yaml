# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2
pro: true

project_name: prowl

variables:
  description: "A tiny terminal radar for your GitHub pull requests."
  homepage: "https://github.com/caarlos0/prowl"
  repository: "https://github.com/caarlos0/prowl"

metadata:
  description: "A tiny terminal radar for your GitHub pull requests."
  homepage: "https://github.com/caarlos0/prowl"
  license: MIT
  maintainers:
    - Carlos Alexandro Becker <carlos@becker.software>
  commit_author:
    name: caarlosb0t
    email: bot@carlosbecker.com

includes:
  - from_url:
      url: https://raw.githubusercontent.com/caarlos0/goreleaserfiles/main/release.yml
  - from_url:
      url: https://raw.githubusercontent.com/caarlos0/goreleaserfiles/main/notary.yml

before:
  hooks:
    - rustup default stable
    - cargo install --locked cargo-zigbuild
    - cargo fetch --locked

builds:
  - builder: rust
    flags:
      - --release
    targets:
      - x86_64-unknown-linux-gnu
      - x86_64-apple-darwin
      - x86_64-pc-windows-gnu
      - aarch64-unknown-linux-gnu
      - aarch64-apple-darwin

# TODO: uncomment when goreleaser fixed
# universal_binaries:
#   - replace: true

archives:
  - formats: [tar.gz]
    # makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

source:
  enabled: true

sboms:
  - artifacts: archive

# Keyless cosign signature of the checksums file.
signs:
  - cmd: cosign
    signature: "${artifact}.bundle"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

# Publish to npm (scoped — the bare `prowl` name is taken).
npms:
  - name: "@caarlos0/prowl"
    repository: "{{ .Metadata.Homepage }}"
    bugs: "{{ .Metadata.Homepage }}/issues"
    access: public
    keywords:
      - github
      - pull-requests
      - merge-queue
      - cli
      - dashboard

homebrew_casks:
  - repository:
      owner: caarlos0
      name: homebrew-tap
      token: "{{ .Env.GH_PAT }}"

nfpms:
  - formats:
      - apk
      - deb
      - rpm

checksum:
  name_template: checksums.txt
